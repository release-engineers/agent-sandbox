#!/bin/bash
set -e

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the agent-test.py from the parent directory
cd "$SCRIPT_DIR/.."
python3 agent-test.py "$@"